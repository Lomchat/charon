# Runtime dependencies installed on each VPS by Charon's bootstrap (the agent
# itself is a stdlib-only zipapp — see agent/build.sh). These are for the
# claude-agent-sdk process that each AgentSession spawns internally.
#
# Charon installs them automatically into ~/.charon/venv/ on the VPS during
# the `install_sdk` phase of `lib/server/claude/bootstrap.ts`. You only need
# to install them locally if you want to run the agent without Charon (e.g.
# for development against a local socket).
claude-agent-sdk>=0.2.110
